SCENARIO_DESCRIPTION="hadoop.env's CORE-SITE.XML_fs.defaultFS disagrees with the actual core-site.xml. Operators relying on env vars get one answer, services reading XML get another."
EXPECTED_RULE=dual-source-consistency
EXPECTED_SEVERITY=warning
EXPECTED_DOWNSTREAM=
