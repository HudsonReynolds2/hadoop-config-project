SCENARIO_DESCRIPTION="dfs.replication (5) > dfs.replication.max (3) — namenode would refuse to honor the requested replication factor"
EXPECTED_RULE=hdfs-replication-max
EXPECTED_SEVERITY=critical
EXPECTED_DOWNSTREAM=
