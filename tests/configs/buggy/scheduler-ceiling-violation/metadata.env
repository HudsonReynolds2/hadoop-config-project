SCENARIO_DESCRIPTION="spark-client's core-site.xml fs.defaultFS points to a different namenode than the cluster — Spark jobs would write to the wrong filesystem"
EXPECTED_RULE=fs-defaultfs-propagation
EXPECTED_SEVERITY=critical
EXPECTED_DOWNSTREAM=
