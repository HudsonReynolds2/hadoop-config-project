SCENARIO_DESCRIPTION="hive.metastore.warehouse.dir points to wrong namenode authority — Hive would write to a non-existent host"
EXPECTED_RULE=hive-warehouse-namenode
EXPECTED_SEVERITY=warning
EXPECTED_DOWNSTREAM=
