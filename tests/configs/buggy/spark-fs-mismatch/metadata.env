SCENARIO_DESCRIPTION="spark.hadoop.fs.defaultFS in spark-defaults.conf points to a different namenode than the cluster's core-site.xml — Spark jobs would write to the wrong filesystem"
EXPECTED_RULE=spark-fs-defaultfs
EXPECTED_SEVERITY=critical
EXPECTED_DOWNSTREAM=
